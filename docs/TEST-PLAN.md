## Test Plan (Phase 1)

### Smoke
- Login → dashboard visible → conversations accessible
- Open existing ticket → verify draft UI (future)

### Agent
- Create ticket via UI → verify in list → open → add note (future)
- Assign/unassign → verify assignee (future)
- Search by subject → validate results (future)

### Admin (trial-permitting)
- Open settings page → basic visibility checks (future)


