{{ko-admin/engagements/form
  theVisitorEngagement=theVisitorEngagement
  definitions=definitions
  automationActionDefinitions=automationActionDefinitions
  registerAs=(action (mut editComponent))
  teams=teams
  agents=agents
  didSave=(route-action "didSave")
  cancel=(route-action "cancel")}}
