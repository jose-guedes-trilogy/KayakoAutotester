{{ko-admin/fields/form
  title=title
  field=field
  editedField=editedField
  schema=schema
  onCancel=(action "canceled")
  onSave=(action "updated")
  onDelete=(action "deleted")
  isApiFieldKeyAvailable=false
  isCustomerEditAvailable=false
}}