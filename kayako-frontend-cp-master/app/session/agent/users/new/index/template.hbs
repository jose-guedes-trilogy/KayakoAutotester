{{ko-user-content
  filter=filter
  onCreateNewCase=(action "createNewCase")
  onQueryParamsUpdate=(action "updateQueryParams")
  openInSameTab=(action "openInSameTab")
  user=model
  postId=postId
  state=state
  tabsComponent="ko-tabs/user"
  userContext="user"
}}
